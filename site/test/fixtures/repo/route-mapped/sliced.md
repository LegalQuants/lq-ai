# Sliced fixture page

Intro paragraph that `from=`/`to=` slicing must drop — it sits before the
`from=` heading, so it never reaches the built page.

## Only this section

This is the only paragraph that should survive the slice.

## Not included

Everything from here on must be excluded by `to=`.
